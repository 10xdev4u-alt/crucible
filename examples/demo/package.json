{
  "name": "crucible-demo",
  "version": "0.1.0",
  "private": true,
  "description": "A small example project for Crucible — used to test the pipeline end-to-end",
  "scripts": {
    "review": "crucible review --diff working",
    "review:full": "crucible review --diff working --verbose",
    "doctor": "crucible doctor"
  }
}
